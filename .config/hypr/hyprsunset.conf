enabled = true

# 全局设置
transition = 1.5          # 切换时的渐变时间（秒），可以改成 2.0 更柔和

# 白天（默认凉色）
profile {
    time = 6:00
    temperature = 6500     # 白天正常色温（偏冷）
    gamma = 1.0
}

# 晚上 6 点后变黄（暖色）
profile {
    time = 18:00
    temperature = 4200     # 比较黄暖的色温（你可以调低到 3800~4000 更黄）
    gamma = 0.95
}
